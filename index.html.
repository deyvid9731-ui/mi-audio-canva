<!DOCTYPE html>
<html>
<head>
    <style>
        body { margin: 0; display: flex; justify-content: center; align-items: center; height: 100vh; background: transparent; overflow: hidden; }
        .play-btn { 
            width: 80px; height: 80px; border: 5px solid white; border-radius: 50%; 
            display: flex; justify-content: center; align-items: center; cursor: pointer;
            background: rgba(0,0,0,0.3);
        }
        .play-btn::after {
            content: ""; border-style: solid; border-width: 15px 0 15px 25px;
            border-color: transparent transparent transparent white; margin-left: 5px;
        }
    </style>
</head>
<body>
    <div class="play-btn" onclick="let a=document.getElementById('aud'); a.paused?a.play():a.pause()"></div>
    <audio id="aud" src="Suerte He Tenido (En Vivo) - Banda Corona Del Rey - Banda Corona Del Rey.mp3"></audio>
</body>
</html>
