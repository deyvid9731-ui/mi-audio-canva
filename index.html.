<!DOCTYPE html>
<html>
<head>
    <style>
        body { margin: 0; display: flex; justify-content: center; align-items: center; height: 100vh; background: transparent; }
        .play-btn { 
            width: 80px; height: 80px; border: 5px solid #fff; border-radius: 50%; 
            background: #000; cursor: pointer; display: flex; justify-content: center; align-items: center;
        }
        .play-btn:after { content: ""; border-style: solid; border-width: 15px 0 15px 25px; border-color: transparent transparent transparent #fff; margin-left: 5px; }
    </style>
</head>
<body>
    <div class="play-btn" onclick="document.getElementById('a').paused?document.getElementById('a').play():document.getElementById('a').pause()"></div>
    <audio id="a" src="musica.mp3"></audio>
</body>
</html>
